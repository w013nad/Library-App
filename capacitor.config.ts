import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourcompany.vervelens',
  appName: 'Library Scanner',
  webDir: 'dist'
};

export default config;
